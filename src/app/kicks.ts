export class Kicks {
  public brand: string;
  public model: string;
  public imageURL: string;
  public cena: string;
  constructor(brand: string, cena: string, model: string, imageURL: string) {
    this.brand = brand;
    this.cena = cena;
    this.model = model;
    this.imageURL = imageURL;
  
  }
}